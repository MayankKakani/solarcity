{{- $corsOrigins := .Values.solarplan.env.corsOrigins -}}
{{- $corsOriginsValue := "" -}}
{{- if kindIs "slice" $corsOrigins -}}
  {{- $corsOriginsValue = join "," $corsOrigins -}}
{{- else if $corsOrigins -}}
  {{- $corsOriginsValue = $corsOrigins -}}
{{- else if .Values.solarplan.env.clientUrl -}}
  {{- $corsOriginsValue = .Values.solarplan.env.clientUrl -}}
{{- end }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "solarplan.fullname" . }}
  labels:
    {{- include "solarplan.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "solarplan.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "solarplan.selectorLabels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ include "solarplan.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: solarplan
          securityContext:
            {{- toYaml .Values.solarplan.securityContext | nindent 12 }}
          image: "{{ .Values.solarplan.image.repository }}:{{ .Values.solarplan.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.solarplan.image.pullPolicy }}
          ports:
            - name: solarplan
              containerPort: {{ .Values.solarplan.service.targetPort }}
              protocol: TCP
          env:
            - name: SOLARPLAN_CLIENT_URL
              value: "{{ .Values.solarplan.env.clientUrl }}"
            - name: CORS_ORIGINS
              value: {{ $corsOriginsValue | quote }}
            - name: AUTH_SECRET
              {{- if .Values.solarplan.env.existingSecret.enabled }}
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.solarplan.env.existingSecret.name }}
                  key: {{ .Values.solarplan.env.existingSecret.key }}
              {{- else }}
              value: "{{ .Values.solarplan.env.authSecret }}"
              {{- end }}
            - name: DATABASE_URL
              {{- if .Values.solarplan.env.database.external.enabled }}
              {{- if .Values.solarplan.env.database.external.existingSecret.enabled }}
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.solarplan.env.database.external.existingSecret.name }}
                  key: {{ .Values.solarplan.env.database.external.existingSecret.passwordKey }}
              {{- else }}
              value: "postgresql://{{ .Values.solarplan.env.database.external.username }}:{{ .Values.solarplan.env.database.external.password }}@{{ .Values.solarplan.env.database.external.host }}:{{ .Values.solarplan.env.database.external.port }}/{{ .Values.solarplan.env.database.external.database }}"
              {{- end }}
              {{- else }}
              value: "postgresql://{{ .Values.postgresql.auth.username }}:{{ .Values.postgresql.auth.password }}@{{ include "solarplan.fullname" . }}-postgresql:{{ .Values.postgresql.service.port }}/{{ .Values.postgresql.auth.database }}"
              {{- end }}
            - name: DISABLE_REGISTRATION
              value: {{ .Values.solarplan.env.disableRegistration | quote }}
            - name: DISABLE_PASSWORD_REGISTRATION
              value: {{ .Values.solarplan.env.disablePasswordRegistration | quote }}
            {{- with .Values.solarplan.extraEnv }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          livenessProbe:
            {{- toYaml .Values.solarplan.livenessProbe | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.solarplan.readinessProbe | nindent 12 }}
          resources:
            {{- toYaml .Values.solarplan.resources | nindent 12 }}

      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
